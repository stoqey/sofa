import 'mocha';
import { expect } from 'chai';

describe('Sofa', () => {
    it('should insert into couchbase', () => {

    })

    it('should get into couchbase', () => {
        
    })

    it('should delete into couchbase', () => {
        
    })

    it('should update into couchbase', () => {
        
    })
})